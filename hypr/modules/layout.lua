hl.config({
    general = {
        layout = "dwindle",
    },

    master = {
        new_status = "master",
    },

    -- Dwindle tuning kept disabled to preserve the current behavior.
    -- dwindle = {
    --     pseudotile = true,
    --     preserve_split = true,
    -- },
})
